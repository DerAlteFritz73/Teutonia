# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'
    trusted_proxies: '127.0.0.1,REMOTE_ADDR'
    trusted_headers: ['x-forwarded-for', 'x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-port', 'x-forwarded-prefix']

    # Note that the session will be started ONLY if you read or write from it.
    session:
        enabled: true
        # Was the PHP default 1440s (24 min), so idle members were dropped fast.
        # Keep idle sessions ~7 days; remember_me re-authenticates beyond that.
        gc_maxlifetime: 604800
        cookie_lifetime: 0 # session cookie; persistence is handled by remember_me

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
